package ch.globaz.eavs.model.eahviv2011000101.v1;

import java.util.HashMap;

class NameSpaces {
    static HashMap mesNS = new HashMap();

    /*
     * static { mesNS.put(nsName, nsValue + "/0");
     * 
     * }
     * 
     * StringBuffer getNS() { for (HashMap) result = result + "xmlns:" + HashMap(i) }
     */
}
